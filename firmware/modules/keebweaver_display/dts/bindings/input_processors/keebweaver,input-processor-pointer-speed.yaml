# SPDX-License-Identifier: MIT

description: Input processor for the KeebWeaver runtime pointer-speed setting

compatible: "keebweaver,input-processor-pointer-speed"

include: ip_zero_param.yaml

properties:
  type:
    type: int
    required: true
  codes:
    type: array
    required: true
